# macOS
.DS_Store

# Secrets. Never commit keys.
.env

# Generated world data. Recreate with: uv run python -m seed.generate
data/cartwheel.db

# Local conversation state and scenario/replay run outputs.
.sessions.db
.sessions.db-*
scenarios/results/
replay/results/

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
scenarios/demo_pilot_gt.json

# Snapshot of the post-pilot world, kept for inspection only.
data/cartwheel-after-pilot.db

# Vendored skill bodies installed with `npx skills add` (HW5 uses
# write-judge-prompt and validate-evaluator). skills-lock.json pins the source
# and hash, so the install is reproducible without committing third-party text.
.agents/skills/
.claude/skills/
